---
description: 子 Agent 使用边界、五分钟心跳、超时恢复、顺序依赖与长任务持续落盘规则
alwaysApply: true
enabled: true
updatedAt: 2026-07-22T00:00:00.000Z
provider:
---

# 子 Agent 执行与心跳规则

本规则适用于本仓库内所有使用子 Agent、Team、异步任务或长时间多阶段执行的工作。目标是避免任务启动后静默、依赖关系被错误并行、子 Agent 中断无人发现，以及主 Agent 将工作流挂在后台等待上。

## 一、启动前必须判断是否应该使用子 Agent

**首次接手概览阶段不得创建子 Agent。** 首次提供仓库地址或仅要求了解项目时，主 Agent 只能按 `AGENTS.md` 的轻量读取协议建立最小背景；不得把概览拆给子 Agent，更不得用子 Agent 执行被禁止的全库扫描、统计或验证。只有用户明确指定后续任务，且任务边界和写入范围已经确定，才按本规则判断是否需要子 Agent。

主 Agent 在创建任何子 Agent 前，必须先写清任务依赖图，并按以下标准决定是否并行。

### 可以并行

仅在满足下列条件时使用子 Agent 并行：

1. 各子任务互不依赖前一任务的结论；
2. 各子任务读取范围相互独立，或只进行只读研究；
3. 各子任务写入不同文件，不会发生覆盖或竞争；
4. 每个子任务有明确输入、输出、验收标准和结束条件；
5. 并行的收益明显高于最终整合与返工成本。

典型适用场景：

- 多个独立模块或多本书的资料检索；
- 多个互不依赖的篇、卷、集大纲生成；
- 独立的统计分析、体系提取、数据核验；
- 不同文件的只读审查。

### 必须顺序执行

出现任一情况时，不得并行：

1. 后一任务依赖前一任务的结论；
2. 多项工作必须共享统一创作视角、人物弧光、伏笔或最终结局；
3. 多个 Agent 会修改同一文件或同一数据区域；
4. 核心架构尚未确定，子任务无法获得稳定边界；
5. 最终结果不能简单合并，需要同一个作者持续保持一致性。

创作类长任务只允许把“资料研究、数据统计、体系提取”并行；世界观决策、总纲、人物弧光、分卷剧情、伏笔和结局必须由一个主创顺序完成。

### 子 Agent 类型选择与创作主控权

1. `code-explorer` 只用于跨文件搜索、读取、统计、定位和已有事实汇总；
2. `code-explorer` 不得承担小说总纲创作、核心架构决策或全书剧情设计；
3. 不得因为某个子 Agent 能生成文本，就把需要长期统一上下文的创作工作分配给它；
4. 即使存在其他通用或创作型子 Agent，下列工作仍必须由同一个主 Agent/主创上下文顺序完成：
   - 400万字等超长篇小说的整体创作决策；
   - 主题、核心矛盾和全书总主线；
   - 主角、核心配角与反派的完整人物弧光；
   - 世界观增量、地图升级和力量体系推进；
   - 全书卷数、各卷功能和卷间因果衔接；
   - 长线伏笔的埋设、推进、误导与最终回收；
   - 情感线、家族线、师门线与战争线的协调；
   - 终局问题、最终大战和圆满结局设计；
   - 全书统一语气、价值观和创作取舍；
5. 上述核心创作工作不得拆给多个 Agent 分别完成后再强行拼接；
6. 子 Agent 返回的创意或结构只能作为研究素材，不能直接视为正式总纲；
7. 主 Agent 必须亲自重新判断、统一设定、解决冲突并写入唯一主产物；
8. 如果已经错误地把核心创作分配给子 Agent，应立即停止继续扩散任务，将已有输出降级为参考资料，由主 Agent重新顺序整合，禁止直接拼稿。

### 创作类长任务的固定顺序

涉及长篇小说、游戏世界观、系列剧本等持续创作时，主 Agent必须在同一上下文中按顺序完成：

1. 固定需求与不可变约束；
2. 确定主题、核心矛盾和圆满结局；
3. 从结局反推主角弧光和终局条件；
4. 确定世界阶段、地图升级和等级进度；
5. 确定总卷数、总章数和字数预算；
6. 确定每卷功能、主要冲突和卷尾钩子；
7. 安排比赛、试炼、排名、爽点和情感节点；
8. 建立伏笔表并检查埋设、推进与回收；
9. 检查人物弧光、卷间因果、终局闭环和圆满结局；
10. 由主 Agent写入最终总纲并统一润色。

前一步未稳定前不得并行展开后一步。资料研究可以提前并行，但不能替代主 Agent的创作决策。

## 二、子 Agent 启动 Prompt 的强制内容

每个子 Agent 的任务说明必须包含：

1. 任务目标和明确边界；
2. 必读文件及读取顺序；
3. 是否允许修改文件；
4. 唯一产出文件或返回结果；
5. 可量化验收条件；
6. 存疑、中断和原始数据异常的处理方式；
7. 本规则第三节定义的五分钟心跳协议；
8. 完成后必须发送最终回报，不得只写文件不通知主 Agent。

若 Prompt 未注入心跳协议，不得启动该子 Agent。

## 三、五分钟心跳协议（最高优先级）

### 3.1 子 Agent 的主动心跳

每个正在运行的子 Agent 必须主动向主 Agent 发送心跳消息：

1. 启动后立即发送一次“已启动”消息；
2. 此后每隔 5 分钟发送一次；
3. 两次心跳的最大间隔不得超过 5 分钟；
4. 即使没有新进展，也必须发送“仍在运行”消息；
5. 遇到阻塞、权限错误、文件缺失、数据冲突、工具异常时立即发送，不等待下一个周期；
6. 完成时发送最终回报，最终回报替代当期心跳。

统一心跳格式：

```text
[HEARTBEAT]
agent: {agent名称}
status: running | blocked | completed
stage: {当前阶段}
progress: {已完成数量}/{总数量}，或明确百分比
current: {当前正在读取、分析或生成的内容}
last_output: {最近写入的文件/最近完成的结果；无则写“无”}
blocker: {无，或具体阻塞原因}
next: {下一步动作}
```

### 3.2 无精确计时能力时的兜底

若运行环境不提供可用的精确计时器，子 Agent 仍不得静默：

1. 每完成一个阶段立即发送心跳；
2. 连续执行 2—3 次工具调用后发送一次心跳；
3. 在预计耗时较长的单次操作前先发送心跳，操作完成后立即再发送；
4. 以上兜底不能用于主动规避“五分钟一次”的目标，只用于运行时无法精确计时的情况。

### 3.3 主 Agent 的心跳台账

主 Agent 必须维护以下字段：

```text
agent / 任务 / 启动时间 / 最后心跳时间 / 当前阶段 / 进度 / 状态 / 产出路径 / 连续失联次数
```

收到心跳或最终回报后立即更新，不得凭印象判断任务状态。

### 3.4 失联处理

1. 超过 5 分钟未收到心跳：标记为 `heartbeat overdue`，立即主动探活；
2. 探活后仍无响应：不得继续静默等待；
3. 连续两次探活无响应，或超过 10 分钟无任何消息：判定为静默中断；
4. 判定中断后必须选择一种恢复动作：
   - 新建 Agent 从最近落盘检查点接续；
   - 主 Agent 接管剩余工作；
   - 若任务已不适合并行，关闭子 Agent 并改为顺序执行；
5. 恢复动作完成前，不得向用户声称任务仍在正常运行。

## 四、主 Agent 不得把主流程挂在后台等待上

1. 子 Agent 运行期间，主 Agent必须继续推进所有不依赖子 Agent 结果的工作；
2. 禁止只发送“已启动，等待回报”后结束执行；
3. 禁止用“静默等待”代替进度管理；
4. 如果当前阶段完全依赖子 Agent，主 Agent 应检查心跳、检查落盘产物或准备后续验收，而不是空转；
5. 子 Agent 返回的是研究素材，不自动等于最终结论；最终决策与整合责任属于主 Agent。

## 五、长任务必须持续落盘和支持断点续作

预计超过 10 分钟、涉及多阶段或最终文档较长的任务，必须：

1. 先确定唯一主产物文件；
2. 每完成一个阶段立即写入主产物或阶段文件；
3. 阶段完成后记录：完成内容、待办、存疑、下一步；
4. 子 Agent 不得只在内存中积累大量结果后一次性交付；
5. 中断重启时先读取已有产物，从最近检查点继续；
6. 临时文件与最终产物必须区分，任务结束后清理无用临时文件。

创作类长任务建议依次落盘：

1. 需求与不可变约束；
2. 资料研究结论；
3. 世界观与等级边界；
4. 主角、主题与圆满结局；
5. 总体阶段和地图；
6. 卷数、章节数与字数预算；
7. 每卷剧情；
8. 比赛/排名、爽点、情感和伏笔分布；
9. 全局一致性校验。

## 六、进度汇报规则

1. 只汇报真实状态，不把“已派发”写成“已开始产出”；
2. 只有收到心跳或看到产物增长，才能声明子 Agent 正常运行；
3. 用户询问进度时，先检查所有子 Agent 最后心跳，再回答；
4. 若存在失联，必须明确写出失联 Agent、最后心跳、已采取的恢复动作；
5. 不输出无信息量的“继续等待”“处理中”等占位回复。

## 七、验收与关闭

每个子 Agent 完成后，主 Agent必须：

1. 核对结果是否满足输入、输出和数量要求；
2. 检查存疑、异常和未完成项；
3. 验证产物文件真实存在且非空；
4. 将可复用结论整合进主产物，不能只停留在 teammate 消息中；
5. 向所有成员发送 `shutdown_request`；
6. 收到关闭响应后删除团队运行时数据；
7. 关闭团队不等于删除 `.codebuddy/rules`，不得删除项目规则目录。

## 八、与现有项目规则的关系

1. 本规则是仓库级通用工作流规则，适用于所有任务；
2. 各书目录中的领域规则继续有效；
3. 出现冲突时，优先遵守更严格的质量、原文真实性和数据边界要求；
4. 原文或数据存在明显问题时，先向用户反馈，不得用脚本强行修补内容；
5. 默认不新增测试文件，除非用户明确要求。

## 九、不可接受的行为

- 启动子 Agent 后不设置五分钟心跳；
- 使用 `code-explorer` 承担总纲、人物弧光、伏笔闭环或结局设计；
- 将超长篇核心创作拆给多个 Agent 后直接拼稿；
- 把有前后依赖的创作工作错误并行；
- 子 Agent 只写文件、不发最终回报；
- 主 Agent 无心跳证据却声称任务正常；
- 长任务不落盘、只依赖会话上下文；
- 子 Agent 失联后无限等待；
- 为了“看起来并行”而创建没有独立价值的子任务；
- 因等待子 Agent 而停止推进主 Agent 可独立完成的工作。
